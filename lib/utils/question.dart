class Question {
  String question;
  bool answer;

/* //创建一个带有参数的构造函数
  Question(String question, bool answer) {
    this.question = question;
    this.answer = answer;
  } */
  Question(this.question, this.answer);
}
